# 2026-08-11 工作日志

## my_ai_town 居民/角色系统诊断（需求：「把人物改成三国人物」）
- 16 名默认居民是**数据驱动**，定义在 `game/world/data/town/resident_catalog.json`（schemaVersion 1）。
  每位 resident 结构：`attributes`(name/gender/age/appearance/desire愿望/personality性格/speech说话方式/interests/customInterests) + `occupation`(name/workplacePlace) + `presentation`(spritePath/portraitPath/locationLabel)。
- 立绘/精灵指向 `assets/characters/resident_2d_rig_v1/wardrobe_v1/classic_sets/runtime_portraits/<id>_front.png`，由 residentId 关联。
  **改造铁律：不要动 residentId 与 portraitPath，否则断引用、旧存档/工作分配(shopOwnerCandidates)失效。**
- 世界观 lore：`game/prompts/background/10_town_common_knowledge.md`（"山坳尽头的小镇…一条规矩：别让别人过不下去"）。
- 兴趣表：`game/world/data/town/interest_catalog.json`（含 photography/baking 等现代兴趣，三国化需替换）。
- 居民提示词编译：`game/agent/prompt/AgentPromptCompiler.gd`（把 attributes+soul_profile 拼进 LLM 系统提示）。
- 改造性质 = 内容/设计任务（design-strategist）；含立绘重制需 art-director + `tools/character-paper-doll-pipeline`。
  保持 residentId 不变 → 仅影响新游戏名册，旧存档不受影响（居民状态独立存于 AgentSaveStore）。
- 注意：本工作副本**不是 git 仓库**（无 .git）；编辑后须用户同步到真实仓库 mewamew/my_ai_town。

## 工作室推进状态（Phase 0→7）
- 阶段诊断：项目处于 Phase 6 打磨、紧贴 Phase 7 发布门槛；评审强度=Lean；优先=先冲发布。
- 已产出：`docs/studio/phase0-diagnosis.md`；`production/release/` 下 6 份发布资产 + LICENSE 草案。
- 发布三阻塞已定两（macOS=B 披露绕过、LICENSE=A 补）；唯一剩余=真实仓库写权（用户待提供 repo URL + gh 写权）。

## 三国人物改造已落地（13:06，A 轻量文案 + 真实三国人物）
- 已用 `resident_catalog.tk_draft.json` 覆盖 `game/world/data/town/resident_catalog.json`（16 人真实三国人物，7女9男：
  女=大乔/蔡文姬/貂蝉/小乔/孙尚香/甄姬/黄月英；男=诸葛亮/黄忠/张飞/马超/赵云/周瑜/刘备/司马徽/关羽）。
- 原文件备份：`resident_catalog.json.bak.orig`（回退：cp 它覆盖回去即可）。
- 校验（python 实测）：residentId/appearance/presentation/workplacePlace 逐字未动，interests 全来自兴趣表且≤3，JSON 合法；仅影响新游戏，旧存档不受影响。
- 交付：`production/three-kingdoms/roster-map.md`（映射+史实依据）。
- 后续可选升级：B 中度主题化（改 town_common_knowledge.md 世界观+职业时代化）/ C 全量重制（paper-doll 立绘三国化，需 art-director）。

## 三国穿越设定 + backstory 端到端接入（端到端接入决策，已验收）
- 用户决策：写 16 人前世记忆 + 开局故事线文档，并把 `backstory` 字段真接入游戏代码，让记忆出现在居民对话里。
- 交付文档：`production/three-kingdoms/opening-storyline.md`（穿越开局故事线）、`character-backstories.md`（16 前世记忆卡 + `BACKSTORY_JSON_BLOCK`，每行 `residentId|||第一人称短记忆`）。
- 代码接入（8 处 GD）：字段须穿透 6 个校验/重建关卡 + 2 处渲染器，否则被剥离/拒绝：
  - 放行白名单：AgentContractIdentity.gd（whitelist+非空校验）、TownWorldRuntime.gd（校验列表+`_saved_profile_attributes`保存重建）、TownWorldOpeningConfig.gd（`_validate_attributes` exact-keys 全 10 键）、TownWorldRestorePeople.gd（expected_profile_keys）、TownNewGameOpeningCompiler.gd（REQUIRED_ATTRIBUTES）。
  - 渲染器：AgentPromptCompiler.gd（`_render_initialization` + `_render_oc_priority_context` 注入「前世记忆（三国）」）、MemoryOrganizer.gd（`_render_initialization`）。
  - TownResidentCatalog.gd：attribute_fields 加 backstory + 新增 `legacy_without_backstory` 变体（让无 backstory 的自定义居民仍能通过 exact 校验）。
- 注入数据：`resident_catalog.json` 16 人 attributes 全部加 `backstory`（60–130 字「我依稀记得…」），residentId/appearance/portrait/workplace 未动。
- lore 重写：`10_town_common_knowledge.md` 加「镇子来历」共识段（众人从很远很乱的年月被一同搬来，旧事只当前身残影，不深究），保留全部功能性规则（公告栏/敲门/作息/山外）。
- 验收：`improve .workbuddy/tmp/verify_tk.py` 重写为端到端校验，19 检查点全过、0 问题（JSON 合法/16 backstory 非空/8 关卡放行/lore 共识/无重复 var）。
- 踩坑：早期补丁脚本在 TownResidentCatalog.gd 引入重复 `var var` 且缩进错位、AgentContractIdentity.gd 的 `errors.append` 未缩进进 if —— 已用行号精确重写修正（repr 核对每处缩进）。
- 仍阻塞发布：真实仓库写权（用户待提供 repo URL + gh 写权）后，release-ops 跑 `gh release create` + quality 跑 `run_complete_formal_release_validation.sh`（含 GDScript 编译）。

## 三国叙事两特性（端到端接入决策，已验收 PASS）
- 用户需求：(A) 与三国人物聊天，他们会说出当年故事与人生大事；(B) 公告板按游戏天数自动写三国时间线。
- 用户决策：结构化人生大事 + 仅关键节点发帖 + 关键节点相对天。
- 设计交付（design_strategist）：`production/three-kingdoms/tk-narrative-design.md`（前身残影风格/触发语境/渐进揭示/life_events→prompt 注入规则）、`tk-timeline.md`（18 事件表 gameDay/era/title/summary/bulletinText + 选型理由）、`tk_timeline.json`（schemaVersion1, publisherId=tk_chronicle, 18 事件 gameDay 3→120, eventId 前缀 tk_）。
- 数据注入：`resident_catalog.json` 16 人 attributes 全部加 `life_events`（3–5 条 `{era,title,summary,trigger}`，置于 backstory 之后）。
- 代码交付（engineering_lead）：
  - `game/world/runtime/social/TkTimelinePublisher.gd`（class_name extends RefCounted）：加载 tk_timeline.json；`publish_due_for_day(world,bulletin,day)`（gameDay==day 时 published_at=(day-1)*1440+480=08:00，双重幂等：内存 `_posted_event_ids` + `_bulletin_already_has()` bulletin 文本去重）；`ensure_posted_upto()` 回填 1..day；`create/restore_save_snapshot()` 持久化已发事件。
  - `TownCommunityBulletinRuntime.gd`：SYSTEM_PUBLISHER_IDS 加 `"tk_chronicle"`（原 `["world"]`）。
  - `TownWorldRuntime.gd`：L340 加 `_tk_timeline_publisher`；L1156-57 存快照；L17499 `_reset_social_runtimes` new()；L2462-64 恢复快照；L6538/L6716 校验+保存 life_events；**day-advance 钩子**（L1828-1856）发帖循环。
  - `AgentPromptCompiler.gd` / `MemoryOrganizer.gd`：`_render_initialization` 加「前世人生大事（三国）：%s」+ `_format_life_events()` 助手。
  - 6 个校验关卡把 `life_events` 加入白名单：AgentContractIdentity.gd:24、TownWorldOpeningConfig.gd:378、TownWorldRestorePeople.gd:337、TownNewGameOpeningCompiler.gd:30、TownResidentCatalog.gd（attribute_fields + legacy_without_life_events 变体 + L202 条件）。
- 验收：`verify_tk.py` 扩至 33 检查点全过、0 问题。
- **关键逻辑 bug（静态校验器查不出，由 lead 代码评审独立抓出并修正）**：day-advance 钩子两处：
  1. 种子天 `_tk_last_day` 原在 `_environment.advance()` 跳到终态后读 `get_time().day` → 永远相等 → 时间线永不发帖。改为从首 tick `int(_absolute_minute(_tk_ticks[0])/1440)` 取。
  2. 浮点除法 `int(_processing_tick_absolute_minute)/1440+1` 除 minute≠0 均得 day+小数。改为 `int(_processing_tick_absolute_minute/1440)+1`。
  - 修正后 engineering_lead Python 推演 day1→day3：day3 事件发帖、day1/day2 跳过、同日不重复；verify_tk.py 仍 PASS。
- 复用经验：**钩子逻辑（尤其依赖 advance() 后时钟跳变 + 整数/浮点除法）必须做推演级烟雾测试，不能只靠静态 grep 校验**。

## README 重写 + 改名「AI 三国小镇」+ 原项目署名（14:32）
- 用户要求：三国修改与原程序合并、加感谢话、改名 AI 三国小镇、必须保留原项目名/贡献人/原程序。
- 合并核实：修改已直接写入 game/ 源文件——resident_catalog.json 16 人全带 backstory+life_events；TownWorldRuntime.gd day-advance 钩子；TkTimelinePublisher.gd 存在；bulletin SYSTEM_PUBLISHER_IDS 含 tk_chronicle；tk_timeline.json 18 事件(gameDay 3→120)。verify_tk.py 33 项 PASS。
- 残留整理：resident_catalog.tk_draft.json → production/three-kingdoms/；resident_catalog.json.bak.orig → production/three-kingdoms/resident_catalog.pre-tk.bak.json（游戏数据目录清空）。
- README.md 重写：标题改 AI 三国小镇；新增「项目缘起 / 改版新增 / 三国叙事系统(表)」；保留 latest-update 标记块；末段「原项目出处与致谢」含原项目(mewamew/my_ai_town 我的AI小镇, 386★/70fork, Godot4.7/GDScript, 创建2026-08-07)、贡献人(mewamew 创建者 + dzhl-jacl 76 commits)、原程序核心功能列表、致谢段、授权说明（上游暂无统一许可证，保留署名不下结论）。
- 原项目元数据来源：GitHub API（gh CLI 不可用，改用 WebFetch 抓取 repos/contributors）。

## 游戏内标题同步更名（15:05）
- 用户「继续」→ 补全真正的游戏改名：之前只改了 README，游戏引擎标题仍是旧名。
- 检索 game/ 内「我的ai小镇」出现点：project.godot:13(config/name)、export_presets.cfg:54(product_name)/55(file_description)/121(package/name)、GameFlowHost.gd:120(INTERNAL_WINDOW_TITLE)/121(FORMAL_WINDOW_TITLE)。
- 全部改为「AI 三国小镇」；verify grep 已无残留（资产清单里的 "AI Town" 是内部元数据，不改）。
- export_presets.cfg 的 package/unique_name="com.aitown.game" 已是干净反向DNS，保留不改。
- README 改版新增清单补一条「游戏正式更名」说明文档与程序名已统一。

## 包名标识更名 + USER.md 转 HTML（16:43）
- 用户确认：把 package/unique_name 也改成贴合新品牌。改 export_presets.cfg:120 → `com.ai3ktown.game`（反向DNS合法；仅导出构建用，不影响桌面运行）。
- 核验全量更名：config/name="AI 三国小镇"、GameFlowHost 两窗口标题="AI 三国小镇"、export_presets product_name/file_description/package.name="AI 三国小镇"、unique_name="com.ai3ktown.game"。TownProviderCredentialStore.gd:273 仅读取 config/name 的兜底默认值，非写死，无需改。
- USER.md（About Your Human）→ 独立 HTML 个人档案页并交付预览；用户随后表示不要该文件，已删除 `USER-Profile.html`。

## 联系原版作者（22:24）
- 用户要求：通知原版作者 mewamew 这是基于其开源项目的衍生改版，并附「如要求删除可随时联系」；HTML 文件去除；README 现有结构（大部分讲本游戏、小段讲上游）用户认可，保持不变。
- 查 mewamew GitHub 资料：email/blog/twitter 均为空，无公开邮箱，无法直接发邮件。
- 尝试用 GitHub 集成在其仓库 my_ai_town 开 Issue 告知 → 403 Resource not accessible by integration（该集成无权在第三方仓库建 Issue）。
- 已将通知草稿存入 `production/three-kingdoms/upstream-notice.md`，待用户提供作者邮箱或手动发送。
